/*!
 * content-disposition
 * Copyright(c) 2014-2017 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict'

/**
 * Module exports.
 * @public
 */
module.exports = contentDisposition;
module.exports.parse = parse;

/**
 * Module dependencies.
 * @private
 */
const { basename } = require('path');
const { Buffer } = require('buffer');

/**
 * Regular expression to match non attribute characters after encodeURIComponent (not including "%").
 * @private
 */
const ENCODE_URL_ATTR_CHAR_REGEXP = /[\x00-\x20"'()*,/:;<=>?@[\\\x7f{}]/g; // eslint-disable-line no-control-regex

/**
 * Regular expression to match percent encoding escape.
 * @private
 */
const HEX_ESCAPE_REGEXP = /%[0-9A-Fa-f]{2}/;
const HEX_ESCAPE_REPLACE_REGEXP = /%([0-9A-Fa-f]{2})/g;

/**
 * Regular expression to match non-latin1 characters.
 * @private
 */
const NON_LATIN1_REGEXP = /[^\x20-\x7e\xa0-\xff]/g;

/**
 * Regular expression to match quoted-pair in RFC 2616.
 * @private
 */
const QESC_REGEXP = /\

